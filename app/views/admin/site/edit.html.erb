<h1>edit Site</h1>
<%= form_for(@site, :url => update_site_path) do |f| %>
  <% if @site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site.errors.count, "error") %> prohibited this admin_topic from being saved:</h2>

      <ul>
      <% @site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>subdomain: <%= f.text_field :subdomain %></div>
  <div>specifical_index: <%= f.text_field :specifical_index %></div>
  <div>forum: <%= f.text_field :forum_id %></div>
  <div>theme: <%= f.text_field :theme %></div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>